import { Card } from "../../components/Card";

export default function Produtos() {
    return(
        <section className="produtos telas centralizar coluna">
           <Card />
        </section>
    )
}